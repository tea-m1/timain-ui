import {Layout} from "@/layout";
import {CommunityPostList} from "./CommunityPostList";

export const Community = () => {
  return (
    <Layout header={null}>
      <CommunityPostList />
    </Layout>
  );
};
