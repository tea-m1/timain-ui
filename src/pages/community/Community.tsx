import {Layout} from "@/layout";
import {CommunityPostList} from "./components";

export const Community = () => {
  return (
    <Layout header={null}>
      <CommunityPostList />
    </Layout>
  );
};
